[project]
name = "voidview-shared"
version = "0.1.0"
description = "Shared code for VoidView client and server"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
    "loguru>=0.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
