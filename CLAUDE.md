# VoidView - 视频主观质量评测系统

## 项目概述

VoidView 是一个基于 PySide6 和 Windows 11 Fluent UI 风格的桌面应用，用于视频转码团队的主观质量评测、评审、数据收集和趋势统计。

## 业务背景

### 评测流程概述

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           实验生命周期                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐         │
│  │ 创建实验  │───▶│ 客观评测  │───▶│ 主观评测  │───▶│ 结果评审  │         │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘         │
│       │              │              │              │                   │
│       ▼              ▼              ▼              ▼                   │
│  定义模板/参数   VMAF/PSNR     静帧评测/盲测    开发+测试评审           │
│                  比特率/性能    截图+标注       上线决策                │
│                                                      │                  │
│                                                      ▼                  │
│                                               ┌──────────┐              │
│                                               │ 上线回填  │              │
│                                               └──────────┘              │
│                                                 看播指标统计             │
└─────────────────────────────────────────────────────────────────────────┘
```

### 核心概念

| 概念 | 说明 |
|------|------|
| **用户 (User)** | 系统使用者，分为管理员(root)和普通测试人员 |
| **客户 (Customer)** | 提出转码需求的业务方 |
| **应用 (App)** | 客户下的具体业务应用，如 third_app |
| **模板 (Template)** | 转码模板名称，如 hd5、uhd、ld5 |
| **实验 (Experiment)** | 一次完整的转码参数调优任务 |
| **实验组 (ExperimentGroup)** | 实验中的单个测试配置，包含具体参数和评测结果 |
| **参考类型** | 有参考(对齐供应商/自对齐)、无参考(全新上线) |

## 技术架构

### 整体架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          VoidView 客户端-服务端架构                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                          客户端 (Desktop App)                          │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │ │
│  │  │                      表现层 (PySide6 + Fluent UI)               │  │ │
│  │  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐           │  │ │
│  │  │  │实验管理  │  │主观评测  │  │评审流程  │  │统计报表 │           │  │ │
│  │  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘           │  │ │
│  │  └─────────────────────────────────────────────────────────────────┘  │ │
│  │                                    │                                   │ │
│  │                                    ▼                                   │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │ │
│  │  │                      本地服务层                                  │  │ │
│  │  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │  │ │
│  │  │  │ API Client   │  │ 本地缓存      │  │ 视频处理 (FFmpeg)    │  │  │ │
│  │  │  │ (httpx)      │  │ (SQLite)     │  │ 截图/标注            │  │  │ │
│  │  │  └──────────────┘  └──────────────┘  └──────────────────────┘  │  │ │
│  │  └─────────────────────────────────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                        │                                    │
│                                        │ HTTP/REST API                      │
│                                        ▼                                    │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                          服务端 (Backend API)                          │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │ │
│  │  │                      API 层 (FastAPI)                           │  │ │
│  │  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐           │  │ │
│  │  │  │认证接口  │  │实验接口  │  │评测接口  │  │统计接口  │           │  │ │
│  │  │  └─────────┘  └─────────┘  └─────────┘  └─────────┘           │  │ │
│  │  └─────────────────────────────────────────────────────────────────┘  │ │
│  │                                    │                                   │ │
│  │                                    ▼                                   │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │ │
│  │  │                      业务逻辑层                                  │  │ │
│  │  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐    │  │ │
│  │  │  │实验服务   │  │评测服务   │  │评审服务   │  │用户/权限服务  │    │  │ │
│  │  │  └──────────┘  └──────────┘  └──────────┘  └──────────────┘    │  │ │
│  │  └─────────────────────────────────────────────────────────────────┘  │ │
│  │                                    │                                   │ │
│  │                                    ▼                                   │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐  │ │
│  │  │                      数据访问层 (SQLAlchemy)                     │  │ │
│  │  │  ┌──────────────┐  ┌──────────────┐                            │  │ │
│  │  │  │ PostgreSQL   │  │ 文件存储      │                            │  │ │
│  │  │  │ (主数据库)    │  │ (截图/附件)   │                            │  │ │
│  │  │  └──────────────┘  └──────────────┘                            │  │ │
│  │  └─────────────────────────────────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 技术选型

#### 客户端 (Client)

| 层级 | 技术 | 用途 |
|------|------|------|
| UI框架 | PySide6 + PyQt-Fluent-Widgets | Windows 11 Fluent UI 风格界面 |
| HTTP客户端 | httpx | 与后端 API 通信 |
| 本地缓存 | SQLite | 离线数据缓存、截图存储 |
| 视频处理 | FFmpeg + python | 视频解码、帧提取 |
| 状态管理 | Pydantic | 数据模型和验证 |

#### 服务端 (Server)

| 层级 | 技术 | 用途 |
|------|------|------|
| Web框架 | FastAPI | RESTful API 服务 |
| ORM | SQLAlchemy 2.0 | 数据库操作 |
| 数据库 | PostgreSQL / SQLite | 持久化存储 |
| 认证 | JWT + bcrypt | 用户认证和授权 |
| 文件存储 | MinIO / 本地文件系统 | 截图、附件存储 |
| 异步任务 | Celery + Redis (可选) | 耗时任务处理 |

### 核心依赖

#### 客户端 (client/pyproject.toml)

```toml
[project.dependencies]
# UI框架
PySide6 = "^6.6"
PyQt-Fluent-Widgets = "^1.4"

# 网络请求
httpx = "^0.27"
websockets = "^12.0"  # 实时通知 (可选)

# 本地存储
aiosqlite = "^0.19"   # 本地缓存数据库

# 视频处理
ffmpeg-python = "^0.2"
Pillow = "^10.0"      # 图像处理

# 工具库
pydantic = "^2.0"
python-dotenv = "^1.0"
loguru = "^0.7"
keyring = "^25.0"     # 安全存储密码/token
```

#### 服务端 (server/pyproject.toml)

```toml
[project.dependencies]
# Web框架
fastapi = "^0.109"
uvicorn = "^0.27"
python-multipart = "^0.0.6"

# 数据库
sqlalchemy = "^2.0"
asyncpg = "^0.29"     # PostgreSQL async driver
alembic = "^1.12"

# 认证
python-jose = "^3.3"  # JWT
bcrypt = "^4.1"
passlib = "^1.7"

# 文件存储
aiofiles = "^23.0"
# minio = "^7.2"      # 对象存储 (可选)

# 工具库
pydantic = "^2.0"
pydantic-settings = "^2.0"
loguru = "^0.7"
```
ffmpeg-python = "^0.2"

# 工具库
pydantic = "^2.0"  # 数据验证
python-dotenv = "^1.0"
loguru = "^0.7"  # 日志
```

## 项目结构

```
VoidView/
├── CLAUDE.md                        # 本文件 - 项目规划
├── README.md                        # 项目说明
├── docker-compose.yml               # Docker 编排 (可选)
│
├── client/                          # 客户端 (PySide6 桌面应用)
│   ├── pyproject.toml
│   ├── src/
│   │   ├── main.py                  # 应用入口
│   │   │
│   │   ├── app/                     # 应用核心
│   │   │   ├── __init__.py
│   │   │   ├── application.py       # QApplication 封装
│   │   │   ├── config.py            # 配置管理 (API地址等)
│   │   │   └── constants.py         # 常量定义
│   │   │
│   │   ├── ui/                      # UI 层
│   │   │   ├── __init__.py
│   │   │   ├── main_window.py       # 主窗口
│   │   │   ├── login_dialog.py      # 登录对话框
│   │   │   │
│   │   │   ├── widgets/             # 可复用组件
│   │   │   │   ├── __init__.py
│   │   │   │   ├── video_player.py  # 视频播放器
│   │   │   │   ├── video_compare.py # 视频对比(并排/分屏)
│   │   │   │   ├── screenshot_tool.py # 截图标注工具
│   │   │   │   └── ...
│   │   │   │
│   │   │   └── pages/               # 页面
│   │   │       ├── __init__.py
│   │   │       ├── experiment/      # 实验管理
│   │   │       ├── evaluation/      # 评测页面
│   │   │       ├── review/          # 评审页面
│   │   │       ├── statistics/      # 统计报表
│   │   │       └── user_management/ # 用户管理 (root专用)
│   │   │
│   │   ├── api/                     # API 客户端
│   │   │   ├── __init__.py
│   │   │   ├── client.py            # httpx 客户端封装
│   │   │   ├── auth.py              # 认证相关 API
│   │   │   ├── experiment.py        # 实验 API
│   │   │   ├── evaluation.py        # 评测 API
│   │   │   └── upload.py            # 文件上传 API
│   │   │
│   │   ├── models/                  # 本地数据模型 (Pydantic)
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── experiment.py
│   │   │   ├── evaluation.py
│   │   │   └── ...
│   │   │
│   │   ├── cache/                   # 本地缓存
│   │   │   ├── __init__.py
│   │   │   ├── database.py          # 本地 SQLite 缓存
│   │   │   ├── cache_manager.py     # 缓存管理器
│   │   │   └── sync_service.py      # 同步服务 (离线支持)
│   │   │
│   │   └── utils/                   # 工具函数
│   │       ├── __init__.py
│   │       ├── video_utils.py       # 视频处理
│   │       ├── export_utils.py      # 导出工具
│   │       └── logging.py
│   │
│   ├── tests/
│   ├── resources/                   # 资源文件
│   │   ├── icons/
│   │   └── i18n/
│   └── data/                        # 本地数据
│       ├── cache/                   # 缓存数据库
│       ├── screenshots/             # 截图临时存储
│       └── exports/                 # 导出文件
│
├── server/                          # 服务端 (FastAPI)
│   ├── pyproject.toml
│   ├── alembic.ini                  # 数据库迁移配置
│   │
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                  # FastAPI 应用入口
│   │   ├── config.py                # 配置 (数据库、JWT等)
│   │   │
│   │   ├── api/                     # API 路由
│   │   │   ├── __init__.py
│   │   │   ├── deps.py              # 依赖注入 (db session, current_user)
│   │   │   ├── v1/
│   │   │   │   ├── __init__.py
│   │   │   │   ├── router.py        # 路由汇总
│   │   │   │   ├── auth.py          # 认证接口
│   │   │   │   ├── users.py         # 用户管理
│   │   │   │   ├── customers.py     # 客户管理
│   │   │   │   ├── experiments.py   # 实验管理
│   │   │   │   ├── evaluations.py   # 评测管理
│   │   │   │   ├── reviews.py       # 评审管理
│   │   │   │   ├── statistics.py    # 统计数据
│   │   │   │   └── files.py         # 文件上传
│   │   │   │
│   │   ├── models/                  # 数据库模型 (SQLAlchemy)
│   │   │   ├── __init__.py
│   │   │   ├── base.py              # Base 类
│   │   │   ├── user.py
│   │   │   ├── customer.py
│   │   │   ├── experiment.py
│   │   │   ├── evaluation.py
│   │   │   └── review.py
│   │   │
│   │   ├── schemas/                 # Pydantic 模型 (请求/响应)
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── experiment.py
│   │   │   ├── evaluation.py
│   │   │   └── ...
│   │   │
│   │   ├── services/                # 业务逻辑
│   │   │   ├── __init__.py
│   │   │   ├── auth_service.py
│   │   │   ├── user_service.py
│   │   │   ├── experiment_service.py
│   │   │   ├── evaluation_service.py
│   │   │   └── statistics_service.py
│   │   │
│   │   ├── repositories/            # 数据访问
│   │   │   ├── __init__.py
│   │   │   ├── base.py
│   │   │   ├── user_repo.py
│   │   │   └── experiment_repo.py
│   │   │
│   │   ├── core/                    # 核心功能
│   │   │   ├── __init__.py
│   │   │   ├── security.py          # JWT、密码哈希
│   │   │   └── exceptions.py        # 自定义异常
│   │   │
│   │   └── utils/                   # 工具函数
│   │       ├── __init__.py
│   │       └── file_storage.py      # 文件存储
│   │
│   ├── alembic/                     # 数据库迁移
│   │   ├── versions/
│   │   └── env.py
│   │
│   ├── tests/
│   │   ├── conftest.py
│   │   ├── test_auth.py
│   │   └── ...
│   │
│   └── storage/                     # 文件存储目录
│       ├── screenshots/             # 截图
│       └── attachments/             # 其他附件
│
└── shared/                          # 客户端和服务端共享代码
    ├── pyproject.toml
    └── src/
        └── voidview_shared/
            ├── __init__.py
            ├── constants.py         # 共享常量
            ├── enums.py             # 共享枚举 (状态、角色等)
            └── types.py             # 共享类型定义
```

## 数据模型

### 核心实体关系

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              用户与权限                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────┐                                                        │
│  │    User     │◀───────────────────────────────────────────┐          │
│  │  (账号系统)  │                                            │          │
│  └──────┬──────┘                                            │          │
│         │                                                   │          │
│         │ 创建/评审/评测                                     │          │
│         ▼                                                   │          │
│  ┌─────────────┐       ┌─────────────┐       ┌─────────────────┐      │
│  │  Customer   │───1:N─│     App     │───1:N─│    Template     │      │
│  └─────────────┘       └─────────────┘       └─────────────────┘      │
│                                                       │                 │
│                                                       │ 1:N             │
│                                                       ▼                 │
│  ┌─────────────┐       ┌─────────────┐       ┌─────────────────┐      │
│  │   Review    │◀──N:1─│ExperimentGrp│◀──N:1─│   Experiment    │      │
│  │  (评审人)    │       │             │       │   (创建人)       │      │
│  └──────┬──────┘       └──────┬──────┘       └─────────────────┘      │
│         │                     │                                        │
│         │                     │ 1:N                                    │
│         │                     ▼                                        │
│         │               ┌─────────────────┐                            │
│         │               │ObjectiveMetrics │                            │
│         │               └─────────────────┘                            │
│         │                     │                                        │
│         │                     │ 1:1                                    │
│         │                     ▼                                        │
│         │               ┌─────────────────┐                            │
│         └──────────────▶│SubjectiveResult │──────────┐                 │
│                         │   (评测人)       │          │                 │
│                         └─────────────────┘          │                 │
│                                                      │ 1:N             │
│                                                      ▼                 │
│                                                ┌─────────────────┐     │
│                                                │   Screenshot    │     │
│                                                └─────────────────┘     │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 主要数据表

#### 0. 用户与权限
```python
# core/models/user.py
class UserRole(str, Enum):
    ROOT = "root"        # 管理员 - 可以创建账号、管理所有数据
    TESTER = "tester"    # 测试人员 - 可以参与评测和评审

class User(Base):
    id: int
    username: str               # 登录用户名
    password_hash: str          # 密码哈希 (bcrypt)
    display_name: str           # 显示名称
    role: UserRole              # 角色
    is_active: bool             # 是否启用
    created_at: datetime
    created_by: int | None      # 创建人ID (root创建的用户)
    last_login_at: datetime | None

# 用户相关数据关联:
# - Experiment.created_by → User.id (实验创建人)
# - ExperimentGroup.assigned_testers → User.id[] (分配的评测人员)
# - SubjectiveResult.evaluator → User.id (评测人)
# - Review.reviewer → User.id (评审人)
```

**账号管理规则:**
- 系统初始化时自动创建 `root` 账号，默认密码需要首次登录修改
- 只有 `root` 角色可以创建新账号
- `root` 可以禁用/启用普通账号
- 所有操作记录操作人ID

#### 1. 客户与应用
```python
# core/models/customer.py
class Customer(Base):
    id: int
    name: str                    # 客户名称
    contact: str | None          # 联系人
    created_at: datetime

class App(Base):
    id: int
    customer_id: int
    name: str                    # 应用名 如 third_app
    description: str | None
```

#### 2. 模板与实验
```python
# core/models/experiment.py
class Template(Base):
    id: int
    app_id: int
    name: str                    # 模板名 如 hd5, uhd
    description: str | None

class Experiment(Base):
    id: int
    template_id: int
    name: str                    # 实验名 如 "xx客户third_app实验"
    status: ExperimentStatus     # draft/running/completed/archived
    reference_type: ReferenceType # supplier/self/new
    created_at: datetime
    created_by: str

class ExperimentGroup(Base):
    id: int
    experiment_id: int
    name: str                    # 实验组名 如 hd5_0215_roi01
    encoder_version: str         # 转码服务镜像版本
    transcode_params: dict       # 转码参数 JSON
    input_url: str               # 入流链接
    output_url: str              # 出流链接
    status: GroupStatus          # pending/objective_pass/subjective_pass/rejected
```

#### 3. 客观指标
```python
# core/models/evaluation.py
class ObjectiveMetrics(Base):
    id: int
    group_id: int
    bitrate: float               # 比特率 kbps
    vmaf: float | None           # VMAF 分值
    psnr: float | None           # PSNR 分值
    ssim: float | None           # SSIM 分值

    # 性能指标
    machine_type: str | None     # 机型 如 c3.8xlarge2
    concurrent_streams: int      # 并发路数
    cpu_usage: float | None      # CPU 使用率
    gpu_usage: float | None      # GPU 使用率

    detailed_report_url: str | None  # 完整指标sheet链接
    created_at: datetime
```

#### 4. 主观评测
```python
# core/models/evaluation.py
class SubjectiveResult(Base):
    id: int
    group_id: int
    evaluator: str               # 评测人员
    evaluation_type: EvalType    # static_frame / blind_test

    # 静帧评测
    has_artifacts: bool          # 是否有马赛克/振铃/伪影
    blur_score: int | None       # 模糊程度 1-5
    artifact_types: list[str]    # 伪影类型列表

    # 盲测
    blind_test_winner: str | None # 实验组/对照组

    notes: str | None            # 备注
    word_doc_url: str | None     # Word文档地址
    created_at: datetime

class Screenshot(Base):
    id: int
    subjective_result_id: int
    file_path: str               # 截图存储路径
    annotation: str              # 标注描述 如 "红框标注马赛克"
    issue_type: str              # 问题类型: blur/mosaic/ringing/artifact
    timestamp: float | None      # 视频时间戳
    created_at: datetime
```

#### 5. 评审与上线
```python
# core/models/review.py
class Review(Base):
    id: int
    group_id: int
    reviewer: str                # 评审人
    review_type: ReviewType      # objective / subjective
    result: ReviewResult         # pass / reject / pending
    comments: str | None
    reviewed_at: datetime

class OnlineMetrics(Base):
    id: int
    group_id: int
    ctr: float | None            # 点击率
    pull_success_rate: float | None  # 拉流成功率
    penetration_rate: float | None   # 渗透率
    collected_at: datetime
```

## 功能模块详解

### 1. 实验管理模块

#### 1.1 实验列表页
- 按客户/应用/模板筛选实验
- 显示实验状态和进度
- 支持创建、编辑、归档实验

#### 1.2 实验详情页
- 实验基本信息
- 实验组列表(表格形式)
- 每个实验组的参数、客观指标、主观评测状态
- 快速跳转到评测页面

#### 1.3 创建实验对话框
- 选择客户/应用/模板
- 设置参考类型
- 批量导入实验组(从Excel或手动添加)

### 2. 评测模块

#### 2.1 客观评测页
- 表格展示所有实验组的客观指标
- 指标对比功能(多个实验组横向对比)
- 指标可视化图表
- 快速标记"通过/不通过"

#### 2.2 主观评测页 (核心功能)

##### 静帧评测
```
┌─────────────────────────────────────────────────────────────────┐
│  主观评测 - 静帧模式                          [全屏] [并排] [分屏] │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────┐  ┌─────────────────────┐               │
│ │      原始视频        │  │      转码后视频      │               │
│ │                     │  │                     │               │
│ │   [视频播放区域]     │  │   [视频播放区域]     │               │
│ │                     │  │                     │               │
│ │  时间: 00:12:34.56  │  │  时间: 00:12:34.56  │               │
│ └─────────────────────┘  └─────────────────────┘               │
│                                                                 │
│  同步控制: ▶️ ⏸️ ⏹️  进度 ═══════════════○═══════  速度: 1x    │
│  时间偏移: [ -0.5s ] [ 0s ] [ +0.5s ]  (针对友商不同FPS)        │
├─────────────────────────────────────────────────────────────────┤
│  📸 截图工具                                                     │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ [选择区域截图] [全屏截图] [标注工具: 🔴红框 📝文字]        │  │
│  │                                                          │  │
│  │ 已截取的图片:                                             │  │
│  │ ┌─────┐ ┌─────┐ ┌─────┐                                 │  │
│  │ │img1 │ │img2 │ │ +   │  点击添加到评测报告              │  │
│  │ └─────┘ └─────┘ └─────┘                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│  评测表单                                                        │
│  □ 模糊  □ 马赛克  □ 振铃效应  □ 色彩失真  □ 轮廓缺失  □ 拖影    │
│  模糊程度: ○1 ○2 ●3 ○4 ○5                                      │
│  备注: [_________________________________________]              │
│                                              [保存评测结果]     │
└─────────────────────────────────────────────────────────────────┘
```

##### 盲测模式
```
┌─────────────────────────────────────────────────────────────────┐
│  盲测 - 请选择观感更好的视频 (实验ID: 盲测-20240215-001)         │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────┐  ┌─────────────────────┐               │
│ │       视频 A        │  │       视频 B        │               │
│ │                     │  │                     │               │
│ │   [视频播放区域]     │  │   [视频播放区域]     │               │
│ │                     │  │                     │               │
│ └─────────────────────┘  └─────────────────────┘               │
│                                                                 │
│  同步播放控制...                                                 │
├─────────────────────────────────────────────────────────────────┤
│  请选择观感更好的视频:                                           │
│  [ 视频 A 更好 ]  [ 视频 B 更好 ]  [ 无明显差异 ]                 │
│                                                                 │
│  (评测完成后，结果将提交给测试组织者汇总)                         │
└─────────────────────────────────────────────────────────────────┘
```

### 3. 评审模块

#### 3.1 评审页面
- 展示待评审的实验组列表
- 关联客观指标和主观评测结果
- 支持多人在评审中添加意见
- 最终决策: 通过/驳回

#### 3.2 评审历史
- 历史评审记录查询
- 按时间/客户/结果筛选

### 4. 统计报表模块

#### 4.1 仪表盘
- 进行中的实验数量
- 待评测/待评审数量
- 近期上线实验组

#### 4.2 趋势分析
- 各模板码率-画质趋势
- VMAF/PSNR 对比图表
- 上线后看播指标追踪

### 5. 数据导出

#### 5.1 Excel 导出
- 实验概览表
- 各模板 Sheet
- 客观指标汇总

#### 5.2 Word 导出
- 主观评测报告
- 包含截图和标注

## 开发优先级

### Phase 1: 基础框架 (MVP)
1. 项目脚手架搭建
2. 数据库模型和迁移
3. 基础 UI 框架 (主窗口、导航)
4. 实验管理 CRUD

### Phase 2: 核心评测功能
1. 客观指标录入和展示
2. 视频播放器组件
3. 视频对比组件 (并排/分屏)
4. 截图标注工具
5. 主观评测表单

### Phase 3: 评审流程
1. 评审流程管理
2. 盲测功能
3. 多人协作支持

### Phase 4: 数据分析
1. 统计报表
2. 趋势图表
3. Excel/Word 导出

### Phase 5: 优化增强
1. 性能优化
2. 用户体验改进
3. 高级搜索和筛选

## 开发规范

### 代码规范
- 使用 `ruff` 进行 linting
- 使用 `mypy` 进行类型检查
- 遵循 PEP 8 编码规范

### Git 规范
- 分支命名: `feature/xxx`, `fix/xxx`, `refactor/xxx`
- Commit 格式: `type(scope): description`

### UI 设计规范
- 遵循 Windows 11 Fluent Design
- 使用 PyQt-Fluent-Widgets 组件库
- 保持界面简洁、一致

## 快速开始

```bash
# 安装依赖
uv sync

# 运行数据库迁移
alembic upgrade head

# 启动应用
uv run python src/main.py
```

## 相关资源

- [PySide6 文档](https://doc.qt.io/qtforpython/)
- [PyQt-Fluent-Widgets](https://github.com/zhiyiYo/PyQt-Fluent-Widgets)
- [SQLAlchemy 文档](https://docs.sqlalchemy.org/)
- [FFmpeg Python](https://github.com/kkroening/ffmpeg-python)
